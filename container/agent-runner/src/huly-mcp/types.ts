// Single source of truth for the MCP tool shape — shared with the built-in
// nanoclaw MCP (same container build, so no runtime-boundary reason to copy it).
export type { McpToolDefinition } from '../mcp-tools/types.js';
